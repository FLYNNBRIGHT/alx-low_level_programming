#include <stdio.h>

/**
 * main - Prints "with proper grammar,..." using printf function.
 *
 * Return: Always 0 (Success).
 */

int main(void)
{
	char Prop_G[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", Prop_G);

	return (0);
}
