#include <stdio.h>

/**
 * main - Prints "Programming is like ...".
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
	char Str_Prnt[] = "\"Programming is like building a multilingual puzzle";

	puts(Str_Prnt);

	return (0);
}
