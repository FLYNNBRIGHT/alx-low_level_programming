#include "main.h"

/**
 * _abs - prints an absolute value and returns the exact value.
 * @int: is the number to be computed.
 *
 * Return: n if integer is absolute else return -n.
 */

int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
