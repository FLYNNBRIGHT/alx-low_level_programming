#include "main.h"

/**
 * add - adds two integers and returns the result.
 * @num1: int to be added to b.
 * @num2: int to be added to a.
 *
 * Return: result.
 */

int add(int, int)
{
	int num1;
	int num2;
	int result;

	result = num1 + num2;

	return (result);
}
